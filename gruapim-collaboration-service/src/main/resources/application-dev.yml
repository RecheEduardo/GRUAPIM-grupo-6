spring:
  datasource:
    # Banco de dados PRÓPRIO do microsserviço (database per service).
    url: jdbc:postgresql://localhost:5432/gruapim_collab
    username: gruapim
    password: gruapim123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    baseline-on-migrate: true

gruapim:
  security:
    jwt:
      # Mesmo segredo do serviço de Identidade (gruapim-backend) — confiança stateless.
      # O microsserviço apenas VALIDA tokens; nunca os emite.
      secret: dev-secret-key-at-least-256-bits-long-for-hs256-change-in-production-now
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:5173"

logging:
  level:
    com.gruapim.collaboration: DEBUG
    org.springframework.security: DEBUG
