spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gruapim_dev
    username: gruapim
    password: gruapim123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    baseline-on-migrate: true

gruapim:
  security:
    jwt:
      secret: dev-secret-key-at-least-256-bits-long-for-hs256-change-in-production-now
      expiration: 86400000
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:5173"

logging:
  level:
    com.gruapim: DEBUG
    org.springframework.security: DEBUG
